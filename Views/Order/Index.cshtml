<h2 class="mb-4">Process Orders</h2>

<div class="card shadow-sm text-center">
    <div class="card-body">
        <p class="lead">Click the button below to process a new order.</p>
        <form asp-action="ProcessOrder" method="post">
            <button type="submit" class="btn btn-danger btn-lg">Process Order</button>
        </form>
    </div>
</div>
